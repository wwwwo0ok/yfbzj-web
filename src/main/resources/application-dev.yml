# 开发环境配置
spring:
  thymeleaf:
    cache: false
  datasource:
    dynamic:
      primary: master #设置默认的数据源或者数据源组,默认值即为master
      datasource:
        master:
          username: bzj
          password: BoZhongJi123
          driver-class-name: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://localhost:3306/bzj?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=GMT%2b8&allowPublicKeyRetrieval=true&useSSL=false
  redis:
    host: 112.124.16.133          # Redis服务器地址
    port: 6379               # 默认端口
    password: yfredis123456  # 密码（如无则删除此行）
    database: 9              # 默认数据库索引
    timeout: 3000            # 连接超时时间（毫秒）
  session:
    store-type: redis        # 启用Redis存储Session
    timeout: 1800           # Session超时时间（秒），默认30分钟
    redis:
      namespace: spring:session  # Redis Key前缀（避免冲突）
sa-token:
  # 使用Redis存储Session
  store: redis
  # Redis连接配置（与spring.redis保持一致）
  redis:
    host: 112.124.16.133
    port: 6379
    password: yfredis123456
    database: 9
  token:
    session-token-timeout: 86400
file:
  #文件上传目录 绝对路径 末尾不需要加 /
  path: D:/files #windows
  #path: /data/files #linux
