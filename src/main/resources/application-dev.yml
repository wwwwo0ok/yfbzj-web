# 开发环境配置
spring:
  thymeleaf:
    cache: false
  datasource:
    dynamic:
      primary: master               # 默认数据源
      datasource:
        master:
          username: bzj
          password: BoZhongJi123
          driver-class-name: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://localhost:3306/bzj?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=GMT%2b8&allowPublicKeyRetrieval=true&useSSL=false
  redis:
    host: 112.124.16.133
    port: 6379
    password: yfredis123456
    database: 9
    timeout: 3000
  # 删除了整个 spring.session 配置块

# Sa-Token 配置（超时时间改为 30 分钟）
sa-token:
  store: redis                      # 使用 Redis 存储 Token
  redis:
    host: 112.124.16.133
    port: 6379
    password: yfredis123456
    database: 9
  timeout: 86400                   # Token 有效期，单位秒（30分钟）
  active-timeout: 86400
  # 如果希望每次操作自动续期，可以去掉 active-timeout 或设置 -1

# 文件上传目录
file:
  path: D:/files                    # Windows 路径（Linux 改为 /data/files）