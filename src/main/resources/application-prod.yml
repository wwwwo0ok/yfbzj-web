# 生产环境配置
spring:
  thymeleaf:
    cache: false
  datasource:
    dynamic:
      primary: master #设置默认的数据源或者数据源组,默认值即为master
      datasource:
        master:
          username: bzj
          password: pnWZLLbpT7x7sCJz
          driver-class-name: com.mysql.cj.jdbc.Driver
          url: jdbc:mysql://172.19.5.64:3306/bzj?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=GMT%2b8
  redis:
    host: 172.19.5.63          # Redis服务器地址
    port: 6379               # 默认端口
    password: yfredis123456  # 密码（如无则删除此行）
    database: 1              # 默认数据库索引
    timeout: 3000            # 连接超时时间（毫秒）
sa-token:
  # 使用Redis存储Session
  store: redis
  # Redis连接配置（与spring.redis保持一致）
  redis:
    host: 172.19.5.63
    port: 6379
    password: yfredis123456
    database: 1
  timeout: 1800 # Token 有效期，单位秒（30分钟）
file:
  #文件上传目录 绝对路径 末尾请加 /
  path: F:/files/ #windows
  #path: /data/files/ #linux
knife4j:
  production: true #生成环境禁用查看文档